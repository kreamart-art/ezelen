{
  "name": "ezelen-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "EZELEN realtime room server (WebSocket + HTTP polling, in-memory)",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=20" }
}
